# Archivo de variables de entorno para Docker
# Copiar a .env y completar con valores reales

# Base de datos (OBLIGATORIO cambiar en producción)
DB_PASSWORD=your_secure_db_password_here

# Claves de seguridad (OBLIGATORIO cambiar en producción)
SECRET_KEY=your-super-secure-secret-key-32-chars-minimum
JWT_SECRET_KEY=your-jwt-secret-key-different-from-above

# Email (OBLIGATORIO para funcionamiento)
MAIL_USERNAME=espaciothaumazein@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here
MAIL_DEFAULT_SENDER=espaciothaumazein@gmail.com

# URL del frontend (para enlaces en emails)
FRONTEND_URL=http://localhost

# Mercado Pago (opcional, para pagos automáticos)
MP_ACCESS_TOKEN=your_mp_access_token
MP_PUBLIC_KEY=your_mp_public_key

# URLs de producción (cambiar dominio) - ahora se auto-generan con FRONTEND_URL
# MP_SUCCESS_URL=https://tu-dominio.com/payment-success
# MP_FAILURE_URL=https://tu-dominio.com/payment-failure
# MP_PENDING_URL=https://tu-dominio.com/payment-pending